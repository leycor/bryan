import React, {Fragment} from 'react'

const TitleSection = ({ title }) => (

    <Fragment>
        <h2 className="font-bold text-lg text-gray-600 mb-4 uppercase "> { title } </h2>
    </Fragment>
)

export default TitleSection