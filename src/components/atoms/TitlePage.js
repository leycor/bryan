import React from 'react'

const TitlePage = ({ title }) => {

    return(
        <p className='text-center font-bold text-2xl text-gray-700 uppercase'>{ title }</p>
    );
}

export default TitlePage;